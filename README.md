# AWS (leaf-shop)

This repository holds the backend and frontend for the Leaf Shop project.

- Backend: Java (Maven)
- Frontend: React

See `backend/` and `frontend/` for service-specific setup.

Generated an initial README and `.gitignore`.
